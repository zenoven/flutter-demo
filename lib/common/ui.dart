import 'package:flutter/material.dart';

final larggerFont = const TextStyle(fontSize: 18.0);
final iconMap = {
  'saved': new Icon(
    Icons.favorite,
    color: Colors.red,
  ),
  'normal': new Icon(
    Icons.favorite,
  ),
};
